{
  "name": "meridian-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end browser tests for the Meridian inventory dashboard (R3 deliverable).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
